{
  "$defs": {
    "CustomerSlide": {
      "additionalProperties": false,
      "properties": {
        "content": {
          "maxLength": 400,
          "minLength": 1,
          "title": "Content",
          "type": "string"
        },
        "headline": {
          "maxLength": 80,
          "minLength": 1,
          "title": "Headline",
          "type": "string"
        },
        "type": {
          "enum": [
            "opening",
            "empathy",
            "transformation",
            "social_proof",
            "invitation"
          ],
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "type",
        "headline",
        "content"
      ],
      "title": "CustomerSlide",
      "type": "object"
    },
    "InvestorSlide": {
      "additionalProperties": false,
      "properties": {
        "content": {
          "maxLength": 400,
          "minLength": 1,
          "title": "Content",
          "type": "string"
        },
        "headline": {
          "maxLength": 80,
          "minLength": 1,
          "title": "Headline",
          "type": "string"
        },
        "type": {
          "enum": [
            "hook",
            "problem",
            "solution",
            "traction",
            "ask"
          ],
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "type",
        "headline",
        "content"
      ],
      "title": "InvestorSlide",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Output of the `pitch` stage: investor and customer decks.",
  "properties": {
    "customer": {
      "items": {
        "$ref": "#/$defs/CustomerSlide"
      },
      "maxItems": 5,
      "minItems": 5,
      "title": "Customer",
      "type": "array"
    },
    "investor": {
      "items": {
        "$ref": "#/$defs/InvestorSlide"
      },
      "maxItems": 5,
      "minItems": 5,
      "title": "Investor",
      "type": "array"
    }
  },
  "required": [
    "investor",
    "customer"
  ],
  "title": "Pitch",
  "type": "object"
}
